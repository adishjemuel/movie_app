<%= react_component('admin/GenresForm', { user: current_user,
                                          token: form_authenticity_token.to_s,
                                          movies: @movies,
                                          genre: @genre, 
                                          success: flash[:successful]
                                        }) %>