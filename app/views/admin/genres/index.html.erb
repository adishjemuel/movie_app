
<%= react_component('admin/Genres', { user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                      genres: @genres,
                                      # total_pages: @movies.total_pages,
                                      # current_page: @movies.current_page,
                                    }) %>