<%= react_component('admin/Dashboard', {user: current_user, 
                                        token: form_authenticity_token.to_s, 
                                        genres: @genres,
                                        members_count: @members_count, 
                                        reviews_count: @reviews_count,
                                        genres_count: @genres.count
                                       })%>