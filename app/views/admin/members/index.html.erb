<%= react_component('admin/Members', { user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                      members: @members,
                                      success: flash[:successful]
                                    }) %>