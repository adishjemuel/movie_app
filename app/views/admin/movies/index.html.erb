<%= react_component('admin/Movies', { user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                      movies: @movies, 
                                      success: flash[:successful]
                                    }) %>