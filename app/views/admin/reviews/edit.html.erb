<%= react_component('admin/ReviewsForm', { user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                      review: @review,  
                                      movies: @movies,
                                      errors: flash[:errors],
                                      success: flash[:successful]
                                    }) %>