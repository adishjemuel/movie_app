<%= react_component('admin/UsersForm', { user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                       u: @user,  
                                       roles: @roles,
                                      success: flash[:successful]
                                    }) %>