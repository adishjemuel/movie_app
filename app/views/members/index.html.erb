<%= react_component('user/Members', { members: @members, 
                                      user: current_user, 
                                      token: form_authenticity_token.to_s, 
                                      pages: @members.total_pages,
                                      current_page: @members.current_page
                                    })%>