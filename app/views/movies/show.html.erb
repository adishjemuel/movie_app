<%= react_component('user/Movie', { user: current_user, 
                                    movie: @movie, 
                                    casts_and_crews: @movie.members, 
                                    reviews: @movie.reviews, 
                                    token: form_authenticity_token.to_s,
                                    on_list: @on_list,
                                    success: flash[:successful], 
                                    success_create: flash[:successful_create],
                                    success_remove: flash[:successful_remove],
                                    failure: flash[:errors]
                                    }) %>
