<%= react_component('user/Review', {user: current_user, 
                                    token: form_authenticity_token.to_s, 
                                    movie: @movie,
                                    success: flash[:successful], 
                                    errors: flash[:errors]
                                   }) %>