<%= react_component('user/Profile', {user: current_user,
                                     token: form_authenticity_token.to_s,
                                     watchlist: current_user.movies.count,
                                     reviews: current_user.reviews.count
                                    }) %>